<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket Created - Atul Limited</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #1f6a72;
            --secondary-color: #2c3e50;
            --accent-color: #3498db;
            --success-color: #2ecc71;
            --warning-color: #f1c40f;
            --danger-color: #e74c3c;
            --light-bg: #f8f9fa;
            --dark-text: #2c3e50;
            --light-text: #7f8c8d;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            padding-top: 60px;
            padding-bottom: 40px;
        }

        .header {
            background: #1f6a72;
            color: white;
            padding: 0.8rem;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
        }

        .header h1 {
            font-size: 1.5em;
            margin-bottom: 0.2rem;
        }

        .header p {
            font-size: 0.9em;
            opacity: 0.9;
        }

        .main-container {
            flex: 1;
            padding: 2rem;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .success-card {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            text-align: center;
            max-width: 600px;
            width: 100%;
        }

        .success-icon {
            font-size: 4em;
            color: var(--success-color);
            margin-bottom: 1.5rem;
            animation: bounce 1s ease;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }

        .success-card h2 {
            color: var(--secondary-color);
            margin-bottom: 1rem;
            font-size: 1.8em;
        }

        .success-card p {
            color: var(--dark-text);
            margin-bottom: 1.5rem;
            line-height: 1.6;
            font-size: 1.1em;
        }

        .ticket-info {
            background: var(--light-bg);
            padding: 1.5rem;
            border-radius: 8px;
            margin: 1.5rem 0;
            text-align: left;
        }

        .ticket-info p {
            margin-bottom: 0.8rem;
            display: flex;
            align-items: center;
        }

        .ticket-info i {
            margin-right: 0.8rem;
            color: var(--primary-color);
            width: 20px;
        }

        .action-buttons {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .download-btn {
            display: inline-block;
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            text-decoration: none;
            border-radius: 8px;
            margin-top: 1rem;
            transition: all 0.3s ease;
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .back-home {
            display: inline-block;
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            text-decoration: none;
            border-radius: 8px;
            margin-top: 1rem;
            transition: all 0.3s ease;
        }

        .back-home:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .footer {
            background: #1f6a72;
            color: white;
            text-align: center;
            padding: 0.6rem;
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            font-size: 0.85em;
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }

            .success-card {
                padding: 1.5rem;
            }

            .success-icon {
                font-size: 3em;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <h1>Atul Limited</h1>
        <p>Support Ticket System</p>
    </header>

    <main class="main-container">
        <div class="success-card">
            <div class="success-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <h2>Ticket Created Successfully!</h2>
            <p>Your support ticket has been created and assigned a ticket number. We will review your request and respond as soon as possible.</p>

            <div class="ticket-info">
                <p>
                    <i class="fas fa-ticket-alt"></i>
                    <strong>Ticket ID:</strong> <%= ticketId %>
                </p>
                <p>
                    <i class="fas fa-clock"></i>
                    <strong>Created At:</strong> <%= new Date().toLocaleString() %>
                </p>
            </div>

            <div class="action-buttons">
                <a href="/download/<%= ticketId %>" class="download-btn">
                    <i class="fas fa-download"></i> Download Ticket
                </a>
                <a href="/index.html" class="back-home">
                    <i class="fas fa-home"></i> Back to Home
                </a>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2024 Atul Limited. All rights reserved.</p>
    </footer>
</body>
</html>
